# LiveKit Agent 核心依赖
# 注意：images 扩展提供 PIL/Pillow 支持，用于图像编码（视觉分析必需）
livekit-agents[elevenlabs,images]>=1.2.9
# minimax 插件
livekit-plugins-minimax>=1.2.9

# 阿里云插件
livekit-plugins-aliyun>=1.2.9

# 阿里云插件依赖
httpx
openai

# 环境变量管理
python-dotenv

# 可选：如果需要使用 VAD (Voice Activity Detection)
# livekit-plugins-silero
