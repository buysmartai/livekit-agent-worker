# LiveKit Agent 核心依赖
# 注意：images 扩展提供 PIL/Pillow 支持，用于图像编码（视觉分析必需）
livekit-agents[elevenlabs,images]>=1.2.9

# MiniMax TTS 自定义插件 (本地安装)
# 在 Docker 中通过 Dockerfile 单独安装
# 本地开发使用: pip install -e ./livekit-plugins-minimax-tts

# 阿里云插件
livekit-plugins-aliyun>=1.2.9

# Google Gemini 插件 - 用于 LLM
livekit-plugins-google>=1.2.9

# OpenAI 插件 - 用于 STT
livekit-plugins-openai>=1.2.9

# Silero VAD - 用于语音活动检测、打断功能和噪音过滤
livekit-plugins-silero>=1.2.9
openai

# HTTP 客户端 - 用于 REST API 调用
httpx>=0.25.0

# Gemini Flash 2.5 - 用于视频记忆分析
google-generativeai>=0.3.0
pillow>=10.0.0
numpy>=1.24.0

# 环境变量管理
python-dotenv
